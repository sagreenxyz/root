---
import Layout from '../layouts/Layout.astro';
const base = (import.meta.env.BASE_URL || '').replace(/\/$/, '');
---

<Layout title="Welcome – Root" protected={false}>
  <div style="text-align:center; padding: 4rem 1rem 3rem;">
    <div style="font-size:3.5rem; margin-bottom:1rem;">⚡</div>
    <h1 style="font-size:2.5rem; font-weight:800; margin-bottom:1rem; line-height:1.2;">Welcome to Root</h1>
    <p style="color:#64748b; font-size:1.15rem; max-width:600px; margin:0 auto 1.25rem;">
      This site is the root hub for everything — it has a <strong>public side</strong> anyone can browse
      and a <strong>private side</strong> for authenticated users with access to encrypted personal content.
    </p>
    <p style="color:#94a3b8; font-size:0.95rem; max-width:500px; margin:0 auto 2.5rem;">
      100% static, hosted on GitHub Pages. No backend. No passwords transmitted.
    </p>
    <div style="display:flex; gap:1rem; justify-content:center; flex-wrap:wrap;">
      <a
        href={`${base}/login`}
        style="display:inline-block; background:#6366f1; color:#fff; padding:0.85rem 2.25rem; border-radius:0.6rem; text-decoration:none; font-size:1rem; font-weight:600; transition:background 0.2s;"
        onmouseover="this.style.background='#4f46e5'"
        onmouseout="this.style.background='#6366f1'"
      >Sign In to Private Area →</a>
      <a
        href={`${base}/public`}
        style="display:inline-block; background:#f1f5f9; color:#1e293b; border:1px solid #e2e8f0; padding:0.85rem 2.25rem; border-radius:0.6rem; text-decoration:none; font-size:1rem; font-weight:600; transition:background 0.2s;"
        onmouseover="this.style.background='#e2e8f0'"
        onmouseout="this.style.background='#f1f5f9'"
      >Browse Public Content →</a>
    </div>
  </div>

  <div style="display:grid; gap:1.25rem; grid-template-columns:repeat(auto-fit,minmax(220px,1fr)); margin-top:2rem;">
    <div style="background:#fff; border:1px solid #e2e8f0; border-radius:0.75rem; padding:1.75rem;">
      <div style="font-size:1.75rem; margin-bottom:0.75rem;">🌐</div>
      <h2 style="font-size:1rem; font-weight:700; margin-bottom:0.4rem;">Public Side</h2>
      <p style="color:#64748b; font-size:0.9rem;">Open articles and information available to everyone — no account required.</p>
    </div>
    <div style="background:#fff; border:1px solid #e2e8f0; border-radius:0.75rem; padding:1.75rem;">
      <div style="font-size:1.75rem; margin-bottom:0.75rem;">🔒</div>
      <h2 style="font-size:1rem; font-weight:700; margin-bottom:0.4rem;">Private Side</h2>
      <p style="color:#64748b; font-size:0.9rem;">Sign in to access your private dashboard and encrypted personal content vault.</p>
    </div>
    <div style="background:#fff; border:1px solid #e2e8f0; border-radius:0.75rem; padding:1.75rem;">
      <div style="font-size:1.75rem; margin-bottom:0.75rem;">🔐</div>
      <h2 style="font-size:1rem; font-weight:700; margin-bottom:0.4rem;">Secure by Design</h2>
      <p style="color:#64748b; font-size:0.9rem;">Credentials are hashed client-side with SHA-256. Content encrypted with AES-256-GCM. No server required.</p>
    </div>
  </div>
</Layout>
